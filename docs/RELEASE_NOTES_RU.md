# 🚀 Релиз 2.1: Analytics & Admin Approval (Final)

Все приоритеты по аналитике и админ-панели закрыты.

## 🛠 Что было сделано (V2.1)

### 1. Robust Analytics (Google / Meta)
*   **UTM Persistence:** Теперь система автоматически:
    *   «Ловит» UTM метки из URL (например, с рекламы).
    *   Сохраняет их в `sessionStorage`.
    *   Прикрепляет их к **КАЖДОМУ** событию (включая создание гига и регистрацию).
    *   Это гарантирует, что конверсия будет атрибуцирована к источнику трафика.
*   **Cookie Consent:** Баннер куки теперь реально управляет статусом согласия в GTM (`consent_update`), а не просто висит для вида.
*   **Meta Pixel:** Добавлена инструкция и хелперы для отправки событий `CompleteRegistration` (Заполнение профиля) и `Lead` (Создание гига).

### 2. Admin Approval Logic (Серверная часть)
*   Реализована каскадная логика одобрения:
    *   Админ нажимает **"Approve"** на Гиге.
    *   Сервер (`/api/admin`) переводит Гиг в статус `active` / `approved`.
    *   Сервер **автоматически** находит владельца (Вендора) и переводит его в статус `active`, если он ждал проверки.
    *   Теперь процесс: `User (Draft)` -> `User (Profile)` -> `Admin (Approve Gig)` -> `User is LIVE`.

## 🧪 Как протестировать

### Сценарий 1: UTM Tracking
1.  Откройте ссылку типа: `http://localhost:3000/?utm_source=test_ad&utm_medium=banner`.
2.  Пройдите регистрацию (`/onboarding`).
3.  В консоли (при включенном `dev` режиме) вы увидите события `📊 Analytics Event: gig_create_submit` с параметрами `utm_source: test_ad`.

### Сценарий 2: Admin Approval
1.  Создайте нового вендора+гиг (статус `pending_review`).
2.  Зайдите в Админку -> Gigs.
3.  Нажмите галочку (Accept).
4.  Зайдите в таб Vendors -> Active. Ваш новый вендор должен появиться там автоматически.

---
*Created by Antigravity AI*
